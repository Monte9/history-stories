# Sprint 14 verdict — Company: remote bodies materialize and walk

Date: 2026-06-12 01:04 UTC. Evaluator run against dev server on localhost:3000, Chromium/SwiftShader via Playwright. Graded against the aligned ACs in BACKLOG.md (alignment: `agent/evals/20260612-002231-sprint-14-criteria.md`). All checks over `?net=local` with 2-3 pages in ONE browser context; ≤ 3 concurrent WebGL pages throughout. Scratch scripts: `agent/evals/tmp/s14-*.js`. Screenshots: `agent/evals/shots/s14-*.png` (crops are 2-4x zooms of the named full-frame shots). Geometry used: `?face=roman` spawns (0,0) heading 0 facing the roman wall (-z); default spawn (-5,0); forward 3 u/s; room x extent ±10.5.

## Hard gates

1. **`pnpm build` exits 0 — PASS.** Compiled successfully, TypeScript clean, 15/15 static pages, exit 0.
2. **Zero console errors on homepage, story page, every touched page — PASS.** Every local-mode session in scripts A/B2/C/D/E/F/G/H (9 observer/peer/mobile/off pages plus joiners) recorded zero console errors and zero pageerrors (each script's `ERR*` outputs all `[]`). Story and `/gallery` at 1280 and 390: zero. Default `/` at 1280/390: zero script errors; only the sprint-13-calibrated exempt class (browser network-layer `wss://` relay failures: 20 at 1280, 16 at 390, `otherErrs: []`, settled `data-net="off"`).
3. **No broken images — PASS.** Story 3/3 imgs naturalWidth > 0 at both viewports, `/gallery` 11/11 at both (REG_STORY_*/REG_GALLERY_* broken: 0). Room is canvas-only; paintings verified rendered in screenshots.
4. **No horizontal overflow at 390px — PASS.** scrollWidth/innerWidth = 390/390 on the room in local mode with a live peer (MOBILE overflow), default homepage (REG_HOME_390), story, and gallery.
5. **Every AC demonstrated by interaction — PASS.** Detail below.
6. **No regressions — PASS.** Homepage room at 1280/390 (`s14-reg-home-*.png`), story (`s14-reg-story-*.png`), gallery (`s14-reg-gallery-*.png`) all render correctly; solo journey affordances (focus outline, prompt, Enter) exercised live in AC5 with a peer present.

## Acceptance criteria

**AC1 — Company visible and identifiable from across the room: PASS.** Pair: observer at (0, 4.73) facing -z, peer's page walked to (0, -3.60): separation **8.33 u**; observer's `.museum-peer` read (0.00, -3.60) = peer's own HUD to **0.000 u**; label `Visitor 2`, color `#2d7d72` (AC1_PAIR). `s14-ac1-pair-9u-1280.png` + crop `s14-crop-pair-figure.png`: full articulated figure — head, torso, both arms with hands, both legs with feet — teal shirt clearly distinct from the local charcoal body, `Visitor 2` pill readable above its head against the wall at that distance, standing dead-center on the x=0 line exactly where the attrs say. Trio: third page joined, observer `data-peers="2"`, chip "3 in the room", nodes `Visitor 2`/`#2d7d72` and `Visitor 3`/`#4f5d9e` (AC1_TRIO; `s14-ac1-trio2-1280.png`, crops `s14-crop-trio-p2/p3.png`) — tints visibly distinct from each other and from charcoal. The 1-in-8 collision occurred in an earlier run (both `#4f5d9e`, run A AC1_TRIO) and a fresh session re-rolled distinct colors, the AC's accepted remedy.

**AC2 — Materialize and vanish staged, never pops: PASS.** Join: node added with `data-peer-state="entering"`, persisting across **3 poll samples over ~600 ms** (60 ms cadence) and 2 MutationObserver updates (added t7440 → live t8197, ~757 ms) before `live` (AC2_ENTER_POLL/MUTS). Close: `leaving` observed before removal on both closes (5 poll samples / mutations leaving t89565 → removed t90024); P3's figure+node gone **2.57 s** after close; last peer: leaving at 285 ms, node and chip both gone at **700 ms**, `data-peers="0"`, chip element removed (AC2_LAST_CLOSE, AC5_ALL_GONE). Visual mid-fade caught twice in the leaving window: `s14-fade2-leave-1-0.png` and `s14-fade2-leave-3-0.png` (state `leaving` recorded at capture) show the figure and label at clearly partial opacity vs the full-presence control `s14-fade2-control-live.png` (crops `s14-crop-fade-*.png`) — the mesh fades, not just the attribute.

**AC3 — The walk reads alive: PASS.** Displacement (MutationObserver on the peer node, observer side): walker displaced **7.99 u** straight; **24 distinct intermediates** strictly between endpoints, **maxStep 0.72 u**, **max regression 0.000 u**, settled to 0.3 u of the walker's final pose **263 ms** after key-up (WALK2; a second 3.6 u run: 10 intermediates, maxStep 0.64, settle ~0.7 s — same shape). Gait: `s14-gait-a-2.png` vs `s14-gait-c-2.png`, recorded peer poses x=-1.40 vs -1.71 (**Δ0.31 u**, both heading 270): visibly different limb configurations — full leg scissor with trailing foot and hanging arm vs rear-foot lift with knee flex and forearm swung forward (crops `s14-crop-gait-a2/c2.png`); `s14-gait-b-1.png` is a clear wide mid-stride with foot off the ground, `s14-gait-d-1.png` a legs-passing phase — no gliding statue. Idle: settle error 0.000 u, **drift 0.000 u over 2 s**, and `s14-crop-idle2.png` shows a grounded stance (both feet down, legs together, arms hanging), not a frozen mid-stride. Turn: walker 180° in place, walker heading 270.0 vs observer `data-peer-heading` 270.4 (**0.40°** ≤ 15°); `s14-crop-turn-before2/after2.png` show the body facing opposite ways at the same spot (TURN).

**AC4 — Teleport rule, relocate never glide: PASS.** Sends neutered on the walker (BroadcastChannel postMessage), walker relocated **7.41 u** during a **2.83 s** gap, then restored. During the gap (27 samples @ ~100 ms): figure frozen at the last sent pose, **drift 0.000 u**, `data-peers` held `"1"` with exactly one node (AC4 gapPeersOk). Relocation: pose went (7.99, 0.24) at t3143 → (0.56, 0.59) at t3217 in **one sampled step**; **0 samples** in the jump interior; relocation complete in the same sample as the first post-gap update (≤ 1.5 s trivially). Staged visually: `s14-teleport-mid-0.png` (crop `s14-crop-tp-mid0.png`) caught the figure and label at partial opacity at the destination, fully opaque by `mid-1`/`after` — fade-in at the new spot, never a rendered mid-room position. Identity byte-identical before/after (`df4ecdaff503fc61`/`Visitor 2`/`#8e4f79`), max node count 1 across the whole maneuver (AC4 identityStable, maxNodeCount).

**AC5 — Chip honest, presence stays scenery: PASS.** Chip: absent on solo `/?net=local` load (AC5_SOLO_CHIP chip:null), "2 in the room" on first join, "**3 in the room**" on second join, decremented on a leave, removed **700 ms** after the last leave (≤ 5 s). `/?net=off` with a live broadcaster in the same context: 80 samples over 8 s — zero `.museum-peer`, zero chip, `data-net="off"`, and `s14-ac5-netoff-1280.png` shows no remote mesh. Non-interference: peer standing at (0, -1.50) directly between the player at (0,0) and the focused painting — `data-focused` held the marcus slug; fresh re-acquisition through the body (turn away → empty → turn back) re-set `data-focused` with `#museum-prompt` at display:flex/473px (REACQUIRED_THROUGH_BODY; `s14-ac5-reacquire-1280.png`; focus outline visible through-body in `s14-ac5-through-body-1280.png`); Enter opened the story URL. Walk-through: player walked z -0.2 → -3.0 straight through the peer at z=-1.5: monotonic progress every 100 ms sample, **max mid-walk stall 0 ms** (AC5_WALK_THROUGH). Immunity: 40 samples over 4 s while the peer walked 2.4 u and turned — local x/z/heading/focused held exactly **one** distinct state (AC5_IMMUNITY allMatchBaseline:true).

**AC6 — Both camera modes, both viewports, nothing regresses: PASS.** Build exit 0. `?cam=first`: `data-cam-mode="first"`, `data-cam-dist="0.00"`; `s14-ac6-first-1280.png` shows the remote figure (2.1 u ahead, label legible) with **no part of the local body**; after `V`: `data-cam-mode="third"`, dist 3.50, `s14-ac6-third-1280.png` shows both bodies. 390x844 with a peer: no overflow (390/390), remote figure + label + chip render (`s14-ac6-mobile-390.png`), touch "Walk forward" hold moved the player **6.45 u** (MOBILE_MOVE). Zero console errors in every local-mode session at both viewports; homepage/story/gallery standing gates pass (hard gates 2-4, 6).

## Sprint goal judgment

This is the outcome Monte asked for, not a letter-pass. A second visitor genuinely materializes (fade-in with a persistent staged state, caught mid-fade on film), stands exactly where the other human's HUD says they are to 0.000 u, and walks with a readable, phase-varying gait — wide scissors, foot lifts, passing phases, arm counter-swing — across small displacements, settling into a grounded breathing idle instead of a frozen stride. Turns track to half a degree. The one discontinuity produced (7.41 u under a 2.8 s send gap) relocated as a fade at the destination with zero rendered mid-room positions: no cross-room glide, no pop, no duplicate, no identity churn. Presence costs the solo room nothing and never touches the player's controls, focus, or HUD. No gliding statues, no dead puppets observed in any of ~40 screenshots or ~200 DOM samples.

## Soft notes (for sprints 15-16)

1. **Chip/wordmark overlap at 390px:** `#museum-presence-count` renders partially behind the HISTORY STORIES header pill on mobile (`s14-ac6-mobile-390.png` top-left) — chip text unreadable at 390. Layout fix; convert to an AC in sprint 16.
2. **Chip decrements at leaving-start, not unmount:** during a fade-out the chip already says the lower count while the ghost is still visible (~400 ms). Cosmetic honesty trade-off; fine, but worth a taste look.
3. **Visitor numbering churns per session** (sprint 13 note 3 confirmed live): a reload re-rolls label and color mid-visit. Sprint 15's story-round-trip rejoin will surface this to real users.
4. **`?net=` still dropped on story Escape-return** (sprint 13 soft note 1 unresolved): sprint 15's rejoin AC will fail in local eval mode unless the transport choice persists across the round trip.
5. **Billboard label sits slightly left of head center at oblique view angles** (`s14-crop-trio-p3.png`); minor, taste-pass item.
6. **Entering fade measured ~750 ms** vs SPEC 12.5's ~400 ms; reads well (no pop), just noting the deviation for the taste pass.

## Overall verdict: PASS

All six hard gates and all six aligned acceptance criteria demonstrated by interaction, with DOM samples, timings, and screenshot evidence at both viewports.
