# Sprint 5 Verdict: Curator's wall and night lighting

Date: 2026-06-11 06:01 UTC. Evaluator run against dev server on localhost:3000, Playwright chromium (swiftshader). Screenshots in `agent/evals/shots/` (gitignored), eval scripts in `agent/evals/tmp/` (untracked working files).

## Hard gates

1. **`pnpm build` exits 0** - PASS. Ran three times: baseline (exit 0, 15 static pages), with the temp pipeline-proof story (exit 0, 16 pages), and after cleanup (exit 0). No errors or warnings beyond normal output.
2. **Zero console errors** - PASS. Console error + pageerror listeners on every page: `/` (1280, 390), `/gallery` (1280, 390), story page `/20260421-053750-...` (1280, 390), `/?face=roman|ramayana|mahabharata|curator` (1280), `/?face=curator` (390), both full round trips (curator and roman: focus, Enter, story page, Escape), and both pipeline-proof loads. Every captured `errors` array was empty.
3. **No broken images** - PASS. `/gallery`: 11 `img` elements, all `naturalWidth > 0`. Story page: 3 carousel `img`s, all `naturalWidth > 0`. `/` renders via canvas (0 `img` elements, walls fully textured in screenshots).
4. **No horizontal overflow at 390px** - PASS. `scrollWidth == innerWidth == 390` on `/`, `/gallery`, the story page, and `/?face=curator`.
5. **Every acceptance criterion demonstrated by interaction** - PASS. Detail below.
6. **No regressions** - PASS. Home, gallery, and story page render at 1280 and 390 (`s5-home-*.png`, `s5-gallery-*.png`, `s5-story-*.png`). Carousel intact: 3 dots ("Show panel 1/2/3", first `aria-pressed=true`), ArrowRight switches to a visibly distinct treatment (`s5-story-1280.png` vs `s5-story-1280-panel2.png`). Gallery view link on `/`, museum link on `/gallery`. Sprint 3 behaviors re-verified: walking away clears `data-focused` and hides the prompt (`display:none`); Enter with nothing focused leaves URL unchanged.

## Acceptance criteria (sprint 5)

1. **Placard readable at `/?face=curator`** - PASS. `s5-face-curator-1280.png` and crop `s5-curator-placard-crop.png`: lit placard reads "HISTORY STORIES / A Night Gallery", legend "↑↓ walk, ←→ turn / Enter — view a painting / Esc — step back / Rome · Ramayana · Mahabharata". All legible under its own light pool.
2. **Wall 4 = exactly 3 most recent with NEW marker** - PASS. `data-wall-curator` = `20260421-053750-the-grandfather-who-could-not-choose-his-side, 20260417-053005-the-brother-who-didnt-sleep, 20260406-165643-the-vulture-who-sat-wingless-for-decades`. Matches frontmatter dates sorted desc (2026-04-21, 2026-04-17, 2026-04-06) with the documented slug-desc tie-break for the two 2026-04-06 stories (165643 vulture beats 164948 plato). Screenshot shows exactly 3 paintings under "NEW ACQUISITIONS", each with a yellow NEW tag.
3. **Acquisition focus / Enter / Escape round trip** - PASS. Walked from the curator spawn to (-10.5, -1.8): `data-focused` = grandfather slug, `#museum-prompt` shown with "The Grandfather Who Could Not Choose His Side — Press Enter to view", painting brightly highlighted (`s5-curator-rt-focused.png`). Enter navigated to the story URL. Escape returned to `/` at (-10.5, -1.81, hd 280.0) vs saved (-10.5, -1.8, hd 279.6): dx=0.00, dz=0.01, dheading=0.4°. Identical flow verified on the roman wall (Marcus Aurelius painting): dx=0.00, dz=0.01, dh=0.0.
4. **Museum-at-night lighting on every wall** - PASS. `s5-face-roman/ramayana/mahabharata/curator-1280.png`: individual light pools above each painting against near-black wall surfaces; tradition-tinted labels (amber ROME, emerald RAMAYANA, blue MAHABHARATA, purple NEW ACQUISITIONS).
5. **Pipeline-proof** - PASS. Added `stories/20260612-000000-eval-temp-pipeline-proof.md` (date 2026-06-12, tradition roman) + copied PNG, ran `node scripts/build-thumbs.mjs` (1 thumb built), `pnpm build` exit 0. Live site: `data-wall-roman` gained the temp slug (4 paintings visible in `s5-pipeline-roman-1280.png`), `data-wall-curator` became temp + grandfather + brother, displacing the oldest acquisition (vulture) - confirmed visually in `s5-pipeline-curator-1280.png` with NEW tags on all 3. Removed the temp md, PNG, and thumb; `pnpm build` exit 0; `git status --porcelain -- stories/ public/covers/` empty; `data-wall-curator` reverted to the original 3 slugs.

## Soft notes (no failures)

- Escape return on the curator run restored heading within 0.4° (roman run: 0.0°). Likely settle-timing in the reading, visually identical; below any user-perceivable threshold but worth knowing the restore is not always bit-exact for heading.
- `data-heading` is not normalized: after a net-zero turn sequence it read `360.0` instead of `0.0`. Focus math is unaffected (focus engaged at heading 0/360 and 279.6), purely a HUD cosmetic.
- `#museum-prompt` textContent concatenates title and hint without a space ("...His Side— Press Enter to view"); rendered as separate styled spans so it looks fine on screen.
- Acquisition caption text at spawn distance is small but consistent with tradition-wall captions; readable once approached.
- Night-lighting pass is consistent across walls; placard purple wash matches the curator identity without clashing with the three tradition tints.

## Overall: PASS

All six hard gates pass with evidence. Sprint 5 acceptance criteria all demonstrated by interaction.
