# Verdict: Sprint 2 — Walking

Date: 2026-06-11 05:04 UTC. Evaluated against `agent/RUBRIC.md` hard gates + Sprint 2 acceptance criteria in `agent/BACKLOG.md`. Dev server localhost:3000, headless Chromium (swiftshader, effective ~2-3.5 fps; HUD sampled via MutationObserver on `#museum-hud` attribute writes, so per-frame deltas below are true frame steps).

## Hard gates

1. **`pnpm build` — PASS.** Exit 0, 15/15 static pages, TypeScript clean (`thumbs: 11 covers`, compile 3.8s).
2. **Zero console errors — PASS.** Five page loads instrumented (homepage desktop x2 contexts incl. `?face=roman` x2, homepage 390px, story page 1280/390) plus the south/west collision runs (`?face=mahabharata`, `?face=curator`): 0 console errors, 0 pageerrors across all.
3. **No broken images — PASS.** Story page: 1 img, `complete && naturalWidth > 0`. Homepage: no `<img>` elements (canvas textures; `data-loaded="true"` reached on every load).
4. **No horizontal overflow at 390px — PASS.** `/`: scrollWidth 390 = innerWidth 390. Story page: 390 = 390. Shots: `shots/s2-home-390.png`, `shots/s2-story-390.png`.
5. **All acceptance criteria demonstrated by interaction — PASS.** Detail below.
6. **No regressions — PASS.** Homepage 1280 (`shots/s2-walk-before-1280.png`) and 390 (`shots/s2-home-390.png`) render the lit museum with header + reachable "Gallery view" link (visible at 390). Story page renders cover, tradition badge, title, prose at both widths (`shots/s2-story-1280.png`, `shots/s2-story-390.png`).

## Acceptance criteria (gate 5 detail)

**ArrowUp/ArrowDown walk — PASS.** Spawn confirmed (-9.5, 0, heading 90). Held ArrowUp 2s: x -9.5 → -7.41, z unchanged (heading 90 = +x, correct); 9 frame samples, all distinct positions, max single-frame step 0.300 units (≤ 0.5, dt is clamped — at ~3.4 fps unclamped dt would be ~0.88/frame). Before/after screenshots differ (buffer-unequal; visually the Ramayana wall paintings are nearer and larger): `shots/s2-walk-before-1280.png` vs `shots/s2-walk-after-1280.png`. ArrowDown 1.5s: x -7.4 → -8.89 (backward), 7 smooth samples, max step 0.3.

**ArrowLeft/ArrowRight turn — PASS.** ArrowRight 1.2s: heading 90 → 129.8 through 6 distinct increasing values, max per-frame step 9.9° (clockwise, per spec). ArrowLeft 1.2s: 130 → 90.2, decreasing (counterclockwise). View rotates: `shots/s2-turn-before-1280.png` (facing Ramayana wall) vs `shots/s2-turn-after-1280.png` (Ramayana wall at left edge, blue MAHABHARATA label swung into view).

**WASD aliases — PASS.** Identical rates and caps to arrows (max step 0.29-0.30 units / 9.9°): W dx +1.19 (forward), S dx -1.19 (back), A heading 90 → 50.2 (ccw), D heading 50 → 89.8 (cw). W also drove all four collision runs at headings 0/180/270.

**Collision — PASS.** All four walls, head-on, held forward until convergence (MutationObserver tracked every frame; bounds never exceeded |10.5| on either axis):
- North (`?face=roman`, heading 0): z → -10.5, dist from wall 1.50 ≥ 1.4, minZ in log -10.5. `shots/s2-collision-north-1280.png` (painting close-up at the clamp).
- South (`?face=mahabharata`, heading 180): z → +10.5.
- West (`?face=curator`, heading 270): x → -10.5.
- East covered by corner run.
- Corner approach (heading 58.3 from center toward NE): slid along the clamp and converged at (10.5, -10.5), 1.50 from both walls; 81 frame samples, max step 0.3, log bounds maxX 10.5 / minZ -10.5. `shots/s2-collision-corner-1280.png`.

**First-visit hint — PASS.** Fresh context: `#museum-hint` visible with text "Arrow keys to walk and turn · Enter to view a painting · Esc to step back" + × close button (`shots/s2-hint-first-visit-1280.png`; also legible at 390px in `shots/s2-home-390.png`). Path A: close button click hides it, `localStorage["museum.hintSeen.v1"] = "1"`, hidden after reload in same context. Path B (second fresh context): single ArrowUp keypress hides it, flag set, hidden after reload.

## Soft notes (no gate impact)

- Hint overlay matches the dark theme: dark pill, bold key names, readable contrast, dismiss affordance. Good.
- Per-frame displacement clamping (0.3 units / 9.9° even at ~3 fps) is the right call; no teleporting on slow frames.
- `shots/s2-collision-corner-1280.png` is near-fully black: standing in the NE corner facing the wall junction sits between two light pools, so nothing in the FOV catches light. Defensible as "museum at night" but a faint corner fill or wall-grazing light would keep the view legible; consider during Sprint 5's atmosphere pass.
- The clamp stops at exactly 1.5 from each wall, comfortably above the 1.4 criterion, and the north-wall close-up still frames the painting without near-plane clipping.

## Overall: PASS

All six hard gates pass. No fixes required.
