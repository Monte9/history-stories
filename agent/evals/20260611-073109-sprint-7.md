# Sprint 7 verdict: The Broad room rework (daylight gallery)

Evaluated 2026-06-11 07:31 UTC against `agent/RUBRIC.md` hard gates + Sprint 7 acceptance criteria in `agent/BACKLOG.md`. Dev server on localhost:3000, chromium headless (`--use-gl=angle --enable-unsafe-swiftshader`), screenshots in `agent/evals/shots/s7e-*`.

## Hard gates

1. **`pnpm build` — PASS.** Exit 0, 15/15 static pages, TypeScript clean ("Compiled successfully in 4.7s", "Generating static pages (15/15)").
2. **Zero console errors — PASS.** Empty error arrays across all four runs: faces sweep (4 room loads), close-up/focus run, full keyboard journey (room → story → room → story), mobile run (/, /gallery, story at 390). `CONSOLE_ERRORS: []` every time.
3. **No broken images — PASS.** Story page: 3 imgs all naturalWidth=1200. /gallery: 11 imgs, 0 with naturalWidth=0. Room textures: `data-loaded="true"` on every load, paintings visibly textured in all shots.
4. **No horizontal overflow at 390px — PASS.** scrollWidth=390 vs innerWidth=390 on `/`, `/gallery`, and the story page.
5. **All sprint ACs demonstrated by interaction — PASS.** Detail per AC below.
6. **No regressions on homepage + story page at both viewports — PASS.** `s7e-face-*-1280.png`, `s7e-mobile-room-390.png` (room); `journey-story-1280.png`, `s7e-story-390.png` (story); `s7e-gallery-390.png` (gallery). Dark site chrome intact on story/gallery pages; carousel, tags, text all render.

## Sprint 7 acceptance criteria

**AC1 (four bright face shots, mean luminance > 100, floor bottom / honeycomb top, no fog/voids) — PASS.**
Mean luminance: roman 179.1, ramayana 175.1, mahabharata 180.1, curator 181.5 (all > 100; sharp stats, luminance = 0.2126r+0.7152g+0.0722b). Region strips: ceiling strip (top 120px) ~214-215, floor strip (bottom 120px) ~150 on every face. All four shots (`s7e-face-{roman,ramayana,mahabharata,curator}-1280.png`) show the glowing honeycomb veil at top of frame, white wall with paintings mid-frame, light seamed concrete floor at bottom. Far walls render at full brightness from room center (no distance haze); no black voids (dark pixels only inside cover art).

**AC2 (legibility on white) — PASS.**
`s7e-closeup-ramayana-wall-1280.png` + crops: wall label "RAMAYANA" in darkened emerald (`s7e-crop-label-ramayana.png`), story placard "The King Who Chose Exile Over a Crown" as dark text on a light hairline-bordered card (`s7e-crop-placard-ramayana.png`), both readable. Curator placard (`s7e-closeup-curator-1280.png`, crop `s7e-crop-curator-placard.png`): light plate, thin dark frame, dark "HISTORY STORIES" title, violet subtitle "A Walking Gallery" (no night reference), readable controls legend (arrows walk/turn, Enter, Esc) and tradition list.

**AC3 (focus highlight in daylight, focus/prompt behavior unchanged) — PASS.**
`s7e-focus-ramayana-1280.png`: focused painting wears a clearly visible emerald outline; unfocused neighbors in the same frame have plain thin dark frames, no outline. Behavior trace: walk-in at heading 90 sets `data-focused="20260331-054450-the-king-who-chose-exile-over-a-crown"` at x=9.07 (distance < 3 rule intact), `#museum-prompt` visible with the story title + Enter hint; turning away (heading 340.2) clears `data-focused` to `""` and hides the prompt (opacity check); Enter with no focus leaves URL unchanged; Enter while focused navigates to the story (`ENTER_NAV_OK`).

**AC4 (regression journey, HUD/`?face=` unchanged, console clean, build green) — PASS.**
Journey log (journey-desktop2.js): spawn (-9.5, 0, 90) → turn → walk to Rome wall (z clamps at -10.5, collision intact) → focus a roman slug → Enter → story → carousel 3 dots cycling with wrap (active 0→1→2→0) → Escape → restored at exactly (-4.18, -10.5, 305.3), delta dx=0 dz=0 dh=0 (within 0.1) → Wall 4 → focus + open a New Acquisition. HUD contract: `data-x/z/heading/focused/loaded/wall-roman/-ramayana/-mahabharata/-curator` all present and live; wall lists 3/5/3 slugs + 3 curator acquisitions, unchanged semantics (`data-pitch` correctly absent until Sprint 8). `?face=` places camera at (0,0) heading 0/90/180/270 per wall. Zero console errors; build green (gate 1).

**AC5 (390px: bright room, legible touch cluster + hint, no overflow) — PASS.**
`s7e-mobile-room-390.png`: room mean luminance 175.9, honeycomb + white wall + floor render at 390. Touch cluster (4 dark circular buttons, aria-labels Turn left/Walk forward/Walk back/Turn right) and the dark hint pill are both visible and legible against the bright canvas (dark-on-light, own dark backgrounds). Press-and-hold forward moved x from -9.50 to -6.20 (`s7e-mobile-room-walked-390.png`). No overflow (390/390).

## Soft notes (do not block; feed the Sprint 10 taste audit)

- **"Gallery view" header link is washed out over the bright room** (`s7e-crop-gallerylink.png`): muted lavender text on a translucent gray pill against the glowing ceiling at 1280. Styled for the dark build; closest thing to near-invisible text in the room. Fix candidate for the taste pass: dark pill + light text like the "HISTORY STORIES" badge, or dark text on a light pill.
- **Focus prompt uses an em dash**: "The King Who Chose Exile Over a Crown— Press Enter to view" (also missing a space before the dash). Violates the no-em-dash convention; Sprint 9 already owns overlay punctuation, fold it in.
- **Hint overlay overlaps the touch cluster at 390x844** (hint rect y 726-812 vs buttons y 700-748; ▲ button occludes part of the hint text) and shows keyboard copy ("Arrow keys", "Enter", "Esc") on touch. Both are already-scheduled Sprint 9 ACs; recorded here so the taste audit does not re-discover them.
- **Broad vibe**: reads bright, airy, evenly lit; honeycomb veil is convincing at room scale, floor seams subtle. Taste-audit flags: (a) walls render light warm gray rather than crisp white at eye level (bare wall ~200 lum near ceiling, dimmer lower; the Broad reads whiter), (b) the Roman and Mahabharata walls (3 paintings each) feel sparse on a 24-unit wall versus the reference's denser hang, (c) the curator placard frame is noticeably chunkier in profile than the painting frames (compare `s7e-closeup-curator-1280.png` vs paintings).
- Tradition tints as darkened label colors (emerald label, violet curator subtitle, blue MAHABHARATA, amber-brown ROME) match SPEC 2.2; no colored light washes observed.

## Overall verdict: PASS

All six hard gates and all five Sprint 7 acceptance criteria pass with evidence. The night-to-daylight rework landed without breaking movement, focus, Enter/Escape persistence, HUD contract, `?face=`, mobile controls, or the build.
