# Sprint 10 Re-Evaluation, Attempt 2 — Verdict

Focused re-check per the attempt-2 brief, evaluated 2026-06-11 (UTC). Scope: AC2 frame-level re-check after the hintVisible sync fix, floor sheen spot check (nice-to-have), and a regression smoke over ACs 1/3/4/5 territory. Dev server localhost:3000, Playwright chromium (swiftshader) at 1280x900. Probes: `agent/evals/tmp/r10k-ac2-frames.js`, `r10l-reveal-shots.js`, `r10l-faces.js`, `r10l-pixels.js`, `r10e-regressions.js`. Shots: `agent/evals/shots/r10k-*.png`, `r10l-*.png`. Judged the artifact only; no diffs or generator notes read.

## Gates

### Hard gate: `pnpm build` — PASS
Exit 0, 15/15 static pages generated (`/`, `/gallery`, 11 story slugs, not-found).

### Hard gate: zero console errors — PASS
Zero console/page errors across every run: 3 fresh-context AC2 frame runs (r10k), 3 reveal-screenshot runs (r10l-reveal), 2 face captures (r10l-faces), and the full interaction regression suite (r10e). All probes log `CONSOLE_ERRORS []`.

### AC2 — No spawn toast pile-up, frame-level — PASS (previously the sole FAIL)
- **Frame-level reveal check, 3 fresh contexts** (r10k-ac2-frames.js): in-page monitor installed pre-navigation samples on every rAF frame AND a 25ms interval, from navigation through the full ~8s reveal plus 4s margin. Results: RUN1 122 samples/8114ms, RUN2 140/7767ms, RUN3 131/8663ms; **prompt-visible samples: 0 in all three runs**; both-visible: 0; overlapping-rect: 0. rAF sampling covers every painted frame, so no frame a user could see went unchecked. `loaded` flips at t=3351-3656ms, hint first visible t=3921-4262ms; the prompt never appears in the gap (the window where attempt 1 caught the flash at t=4109-4237ms).
- **Mid-reveal screenshots**: organic captures at lum 124 (`r10l-reveal-1-t5365-lum124.png`) and lum 165 (`r10l-reveal-1-t4829-lum165.png`). Lum 124 is the same brightness as attempt 1's failing frame (r10-fade-mid-0-lum123.png, amber prompt over garbled hint). Both new frames show the hint pill alone at bottom center, no prompt, no overlap.
- **Steady state**: all 3 runs end with hint visible at (320,806 640x62), prompt hidden (display:none, rect 0x0), `data-focused` = plato slug (`r10k-steady-hint-1280.png`).
- **Dismiss**: KeyG hides the hint and the prompt appears at (401,826 478x42) reading "The Man Who Read Plato Before He Died · Press Enter to view" for the still-focused painting, all 3 runs (`r10k-afterdismiss-1280.png`).

### Regression smoke (ACs 1/3/4/5 territory) — PASS
- **Walls (AC1)**: 5x5 samples at 4 bare-wall points on `?face=roman` and `?face=mahabharata` after full reveal all read rgb(242,241,237), inside the 225-248 band (`r10l-face-roman-1280.png`, `r10l-face-mahabharata-1280.png`, r10l-pixels output).
- **Spawn composition (AC5)**: default spawn after reveal (`r10l-spawn-steady-1280.png`): facing-wall center painting at x=880 spans rows 360-496 = 137px; view-centered painting (x 560-680) rows 356-500 = 145px. Identical to attempt 1, both ≥ 100px.
- **Wheel**: wheel(0,-300) = 0.90 units forward, wheel(0,300) = 0.89 back, page scroll stays (0,0); wheel(240,0) turns heading 90 → 116.5.
- **Drag**: 200px right drag = heading 117.3 → 169.1 (+51.8°, ~0.26°/px); pitch clamps at exactly -25 and +25; URL unchanged during drag.
- **Gaze handoff**: `?face=roman` center focuses marcus-aurelius; turning right hands off marcus → soldier-who-wept → cleared (""); at heading 50 the next wall's painting (brother-who-didnt-sleep) focuses with prompt visible. Collision still converges at z=-10.5 against the wall at -12 and slides at the corner (x=1.52).
- **Enter/Escape round trip**: from walked-to spot (0, 4.49, heading 180) on `?face=mahabharata`, Enter navigates to `/20260331-055506-the-night-rage-burned-everything`, Escape restores dx=0.000 dz=0.000 dh=0.00.

## Nice-to-have spot check (not gating)

- **Floor sheen: still misses the ≥20 target.** 5px-wide vertical luminance column on `?face=roman` after full reveal at x=350, y 620-800: min 164.0, max 180.4, **range 16.4** (confirmation column at x=900: range 15.8). The smooth gloss component is only ~6.5 levels (180.4 at y=620 falling to ~173.9 at y=800); the rest of the range is a single seam dip to 164.0 at y=706. Improved vs attempt 1 (range 2-16, now consistently at the top of that band) but short of 20. Seams retained: vertical-column dip at y=706 plus a lateral seam dip at x=640 in the y=700 row scan. Report only, per the decision rule.
- Known open nice-to-haves unchanged and non-gating: walls perfectly uniform (corner fusion), plato cover's baked-in "THE SILENT SENATE" title vs placard mismatch (content pipeline).
- No NEW blocks-shippable issue observed in any capture: reveal frames, steady spawn, both face shots, and the regression run all look consistent with the attempt-1 passing state.

## OVERALL: PASS

AC2 now holds at frame level on three fresh contexts with zero prompt-visible samples across every painted frame of the reveal, and the mid-reveal screenshots that reproduced the attempt-1 blocker image are now clean. ACs 1/3/4/5 smoke shows no regression (walls in band, spawn composition unchanged, wheel/drag/gaze/round-trip exact, zero console errors, build green). Floor sheen remains the one un-landed nice-to-have (range 16.4 vs ≥20); it does not gate.
