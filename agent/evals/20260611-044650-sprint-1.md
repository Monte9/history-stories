# Verdict: Sprint 1 — "The room exists"

Evaluated 2026-06-11 04:46 UTC against `agent/RUBRIC.md` hard gates + Sprint 1 acceptance criteria in `agent/BACKLOG.md`. Dev server: localhost:3000. Playwright chromium with `--use-gl=angle --enable-unsafe-swiftshader`.

## Hard gates

1. **`pnpm build` exits 0** — PASS. Thumb prebuild ran ("thumbs: 11 covers, 0 (re)built"), Next 16.2.1 compiled clean, 15/15 static pages generated, exit 0.
2. **Zero console errors** — PASS. Console + pageerror captured on `/`, `/?face=roman`, `/?face=ramayana`, `/?face=mahabharata` (1280x900), `/` (390x844), `/gallery` (both viewports), and story page `/20260421-053750-the-grandfather-who-could-not-choose-his-side`. All arrays empty on every page.
3. **No broken images** — PASS. `/gallery`: 11 `img` elements, all `naturalWidth > 0`. Story page: cover img `naturalWidth` 1200. `/` uses WebGL textures (no `img` elements); painting visibility verified in screenshots instead.
4. **No horizontal overflow at 390px** — PASS. `document.documentElement.scrollWidth` = 390 = `window.innerWidth` on `/`, `/gallery`, and the story page.
5. **All acceptance criteria demonstrated by interaction** — PASS (detail below).
6. **No regressions on homepage + story page at 1280/390** — PASS. Shots: `shots/sprint1-home-1280.png`, `sprint1-home-390.png`, `sprint1-story-1280.png`, `sprint1-story-390.png`. Story shows cover, badges, title, body at both widths.

## Acceptance criteria

- **Museum renders, lit walls per face** — PASS. Canvas rect = full viewport (0,0,1280x900; also 390x844). Screenshots `sprint1-home-1280.png` (RAMAYANA wall + side walls), `sprint1-face-roman-1280.png` (ROME label, amber, 3 paintings), `sprint1-face-ramayana-1280.png` (RAMAYANA label, emerald, 5 paintings), `sprint1-face-mahabharata-1280.png` (MAHABHARATA label, blue, 3 paintings). Pixel stats confirm non-blank content (max RGB 255; distinct paintings, frames with light pools, labels visible in every shot).
- **HUD wall data matches `stories/`** — PASS. `#museum-hud[data-loaded="true"]` reached on all four museum pages. `data-wall-roman` = 3 slugs, `data-wall-ramayana` = 5, `data-wall-mahabharata` = 3; slug-by-slug identical to the 11 markdown files' `tradition:` frontmatter (roman 3 / ramayana 5 / mahabharata 3).
- **Thumb textures only** — PASS. Network log on each museum page: 11 requests to `/covers/thumbs/*.webp`, zero requests matching `/covers/*.png`. Largest thumb on disk: 107,258 bytes (≤ 200 KB); all 11 thumbs present.
- **Gallery + cross-links + story pages** — PASS. `/gallery` renders the card grid (11 cards, tradition badges, blurbs, hover classes; tradition count chips Rome 3 / Ramayana 5 / Mahabharata 3 matching frontmatter). Clicked "Gallery view" on `/` → URL became `/gallery` (verified at 1280 and 390). Clicked "Enter the museum" on `/gallery` → URL became `/`, canvas mounted, `data-loaded="true"` again. Clicked a card → story page rendered with correct `<h1>`.
- **390px museum** — PASS. Canvas 390x844 fills viewport, "Gallery view" link fully inside the viewport (rect x269.8 y16, 104x30) and click-navigates; no overflow (`sprint1-home-390.png`).

## Soft notes (not failing)

- The scene is extremely dark: mean screenshot brightness 3–6 / 255. Labels and paintings read fine, but the per-painting title placards under each frame are illegible at 1280 (e.g. `sprint1-face-ramayana-1280.png`). Wall/floor surfaces are nearly indistinguishable from the black void outside the light pools. Sprint 5's atmosphere pass should raise placard legibility rather than darken further.
- Tradition colors respected end to end: amber ROME, emerald RAMAYANA, blue MAHABHARATA labels in-room match the gallery badge colors.
- The roman wall label reads "ROME" rather than "ROMAN"; consistent with the gallery chip ("Rome"), so treated as the tradition label.

## Overall: PASS

All six hard gates pass; all five Sprint 1 acceptance criteria demonstrated by interaction. Screenshots under `agent/evals/shots/sprint1-*.png`.
