# Sprint 15 verdict — Presence hardening: reconnects, idle, a handful at once

Graded against the agreed criteria in `agent/evals/20260612-011434-sprint-15-criteria.md` (full canonical text) per `agent/RUBRIC.md`. All checks driven live via Playwright against localhost:3000; wire format sniffed from a real `?net=local` page before any synthesis (`{from, msg:{t:"st", d:{x,z,h,p,s}}}`, 2-decimal numbers, confirmed); at most 3 WebGL pages concurrent at any point. Scripts in `agent/evals/tmp/s15-*.js`, screenshots in `agent/evals/shots/s15-*`.

## Acceptance criteria

1. **Story round trip rejoins, both directions, no ghosts — PASS.** A and B mutually visible (`data-net="local"` both, ids a4fc03/5c63c2). Two consecutive trips, B sampled at ~300 ms with MutationObserver: `data-peers` only ever "0"/"1", max 1 `.museum-peer` node at any moment (30 samples/trip, maxNodes 1, badDp 0). A's despawn clean both trips: `leaving` at +2.2 s, node removed +2.7 s after Enter. Re-entry staged both trips: `entering` added then `live` ~400 ms later (t=9766→10282 ms trip 1), B re-showed A's node 1681 ms (trip 1) / 2049 ms (trip 2) after A's `data-loaded`, at A's restored pose exactly (dx=dz=0.000 vs A's HUD; A pose restore itself exact: pre=post (-5,-1.50) and (-5,0.60)). Node tracked A's subsequent walk (8 and 7 fresh intermediates, settle dist 0). On A after Escape: `data-net="local"`, exactly one node bearing B's unchanged session id (5c63c2, FINAL bIdStable:true), B's movement streamed to A (10 and 8 fresh intermediates). Zero WebSocket/external requests on A across both returns (T1/T2_A_EXTERNAL_NET []), zero exempt-class or any console noise after return, zero console errors both pages. No count creep: FINAL both sides `data-peers="1"`, `data-peers-total="1"`, one node, chip "2 in the room". Shots: `s15-ac1-a-after-2trips-1280.png`, `s15-ac1-b-after-2trips-1280.png`.

2. **Transport memory has exactly one shape — PASS.** (a) `/?net=off` round trip: `data-net="off"` before and after Escape-return, `data-peers="0"`, chip absent, zero presence network requests after return (5 s watch, empty), pose restored (dz 0.01). `?cam=first` round trip: `data-cam-mode="first"` before and after, pose restore dx=0/dz=-0.01/dh=0. (b) Explicit param always wins with session history present: `/?net=local`→local, then `/?net=off`→off, then `/?net=local`→local, same browser context. (c) Fresh paramless tab in the context that just ran local sessions settled `data-net="webrtc"` (never "local"), `data-peers="0"`, no chip, zero non-exempt console errors. (s15-ac2.js output.)

3. **Long idle in a background tab is rock-solid; closing it still cleans up — PASS.** B walked into view, then A foregrounded via `bringToFront()` and B untouched 65 s. A sampled every 1 s: 66/66 samples `data-peers="1"`, single `data-peer-id` (43976f), `data-peer-state="live"` at every sample, zero lifecycle transitions and zero add/remove mutations (flicker impossible to miss: MutationObserver ran the whole window), pose drift 0.0000 u. Screenshot at 60 s shows the labeled figure grounded in idle stance with chip "2 in the room" (`s15-ac3-idle-60s-1280.png`). End of window: both pages `data-peers="1"`. Closing backgrounded B: A removed node and chip in 583 ms (< 10 s); further 15 s watch (31 samples): zero re-materialization, `data-peers="0"`, chip null, zero console errors (`s15-ac3-after-close-1280.png`). Sandbox caveat recorded: headless pages report `visibilityState:"visible"`, so OS-level tab throttling could not be induced; the agreed bringToFront mechanism was applied and CPU contention measurably slowed B's timers (recon: 100 ms interval fired at ~4 Hz), approximating the degraded-sender condition.

4. **Reload-teleport relocates, never glides, never doubles — PASS.** Note: `?face=` spawns sit at room center (0,0) — a first run from the roman face spawn was 0 u away and discarded as not meeting the precondition. Rerun: A at (-5,-2.91), 5.79 u from the mahabharata spawn, B watching A's figure (`s15-ac4-b-watching-old-1280.png`). On reload to `/?face=mahabharata&net=local`: B sampled at ~120 ms plus full attribute-mutation log — `data-peers` never exceeded "1", max concurrent nodes 1, zero samples with dp>1. Old node `leaving` at 871 ms, removed at 1034 ms after reload start (< 10 s, clean). New node (new session id a35423, acceptable per AC) added staged `entering` at 6985 ms → `live` at 7487 ms; first rendered pose (0.00, 0.00) = dist 0.000 to A's post-reload HUD spawn (≤ 1 u). Interior ban: 0 offending positions across both the sampler and every data-peer-x/z mutation (nothing > 1.5 u from both endpoints). Figure visible at the new spawn in `s15-ac4-b-new-wall-1280.png`. Zero console errors.

5. **A handful at once, and the cap accounts honestly — PASS.** Observer O + two real museum pages (P1, P2) + protocol-conformant senders from a same-origin /gallery page (equivalence per agreed verification context; wire format sniffed first). S1 and S2 started in the same evaluate call, O reached `data-peers="4"` 596 ms later — arrival-order race exercised, and all four labels DISTINCT (Visitor 2/3/4/5), all four `data-peer-color` values in the spec palette (#5b7793, #4f5d9e, #b85c38, #b06161), chip "5 in the room", `data-peers-total="4"`. Four labeled figures placed apart in `s15-ac5-four-1280.png`. Movement under load: O held forward 3.8 s → 4.00 u displacement (≥ 2 u), zero pose stalls (all 10 samples distinct at ~440 ms effective cadence, far under the 1.5 s stall bound), while P1 streamed 7 fresh intermediates (≥ 3). Nine identities: `data-peers` exactly "8" at every sample over a 5 s watch (dpVals ["8"], maxNodes 8), `data-peers-total="9"`, chip "10 in the room" — counts everyone, no understatement — zero console errors (`s15-ac5-nine-cap-1280.png`). Stopped a RENDERED synthetic sender (no bye, the harder timeout path): previously-unrendered ninth identity (e7f701) appeared staged (`entering` at 6113 ms → `live` at 6698 ms), promotion complete at 7013 ms (< 10 s), `data-peers` back to "8", chip "9 in the room". Tracked surplus is promoted, not dropped.

6. **Mobile holds with company; solo and the site regress nowhere — PASS.** 390x844 `/?net=local` with a rendered peer (chip "2 in the room", 1 node): scrollWidth 390/390; touch cluster walked the player 6.19 u; pairwise bounding-box intersection of chip/wordmark/nav pill/hint/prompt/cluster checked at both observed states (hint-visible and prompt-focused): zero overlaps — chip (16,64) sits clear below the wordmark (bottom y=45); the sprint-14 overlap is fixed; chip text fully legible in `s15-ac6-mobile-hint-390.png` and `s15-ac6-mobile-focus-390.png`. Zero mobile console errors. Solo `/?net=off`: full sprint-12 journey (walk, focus transition → `#museum-prompt` display:flex with "Press Enter to view", Enter → story, Escape → exact pose restore) with zero `.museum-peer` across all samples. Production default bare `/`: settled "webrtc", same journey clean, zero peers ever, only exempt-class wss errors (12-15, default settling path only). `pnpm build` exit 0. Standing gates: story 3/3 and gallery 11/11 images naturalWidth > 0, no overflow at 390 on any page, homepage/story/gallery render correctly at both viewports (`s15-ac6-home-loaded-1280/390.png`, `s15-ac6-story-1280/390.png`, `s15-ac6-gallery-1280/390.png`).

## Rubric hard gates

1. `pnpm build` exits 0 — **PASS** (compiled clean, TypeScript clean, 15/15 static pages).
2. Zero console errors on homepage, story page, every touched page — **PASS** (zero across all ?net=local/off sessions at both viewports; only the calibrated exempt wss class on the bare-/ settling path).
3. No broken images — **PASS** (story 3/3, gallery 11/11; room is canvas, verified by screenshot).
4. No horizontal overflow at 390px — **PASS** (390/390 on room-with-company, story, gallery, home).
5. Every AC demonstrated by interaction — **PASS** (all six above, live DOM + screenshots, no code reading).
6. No regressions on homepage and a story page at 1280/390 — **PASS** (loaded-room and story shots both viewports; sprint-12 journey intact under company and solo).

## Overall: PASS

The sprint goal is met in substance, not just letter: the round trip never orphans either side (and the sprint-13 param-stripping defect is demonstrably gone), an hour-long-feeling idle never flickers, reloads relocate without ghosts, and the crowd accounting (cap, chip, promotion) never lies about who is in the room.

## Soft notes for sprint 16 (taste pass candidates)

1. **Near-camera labels balloon.** Walking through/standing inside a peer renders its label pill across half the screen (mobile focus shot; nine-cap shot's "Visitor 3"). Distance-based label scale or fade-out inside ~1 u would fix it.
2. **Label occlusion in groups.** In the four-peer composition one figure's label hides behind a nearer body (Visitor 4 in `s15-ac5-four-1280.png`). Acceptable geometry, but a taste pass could bias label heights or depth-test them.
3. **Spawn-focused painting shows no prompt.** The prompt renders during the loading screen, then hides; after load the already-focused spawn painting offers no "Press Enter to view" until focus changes away and back. Contract-intact but a fresh visitor gets no affordance for the first thing they look at.
4. **Visitor numbering churn across round trips** (same human becomes Visitor 3 then Visitor 4 on B) — already on record as a sprint-16 item; confirmed observable in AC1.
5. **Narrow-desktop hint copy mismatch:** at 390 px without touch, the hint says "Arrow keys or scroll" while the on-screen button cluster is also shown.
6. **AC3 environment honesty:** headless pages all report `visibilityState:"visible"`, so true background-tab timer throttling could not be induced; evidence rests on the agreed bringToFront mechanism plus measured CPU-contention slowdown of the idle page's timers (~4 Hz effective).
