# Sprint 8 verdict, attempt 2: Trackpad movement and gaze focus

Re-evaluation 2026-06-11 08:14 UTC, scoped per the re-eval brief: re-run the attempt-1 failing probe (initial-focus highlight on fresh load, hard gate 6) plus a regression smoke of the attempt-1 PASS evidence. Attempt 1: `20260611-075922-sprint-8.md` (gates 1-5 PASS, gate 6 FAIL). Fix under test: `src/components/museum/MuseumRoom.tsx` `register()` (lines 314-325) now syncs a freshly registered painting's highlight to the live `data-focused` value. Dev server localhost:3000, chromium headless (`--use-gl=angle --enable-unsafe-swiftshader`), scripts `agent/evals/tmp/s8a2-*.js` + reused `s8-wheel.js` / `s8-drag.js` / `s8-gaze.js`, screenshots `agent/evals/shots/s8a2-*`.

## Hard gates

1. **`pnpm build` — PASS.** Exit 0: "Compiled successfully in 4.9s", TypeScript clean, 15/15 static pages.
2. **Zero console errors — PASS.** Empty error arrays on all 5 fresh face loads (`s8a2-initial-focus.js`), wheel suite, drag suite, gaze suite (includes story page after Enter), clean round trip (`s8a2-roundtrip.js`), story page at 1280 and 390, home at 390 (`s8a2-smoke.js`).
3. **No broken images — PASS.** Story page (armor-karna): 3/3 imgs naturalWidth 1200. Homepage shell has no `<img>`; room textures load (`data-loaded="true"` on every run, textured paintings in all `s8a2-*-initial.png`).
4. **No horizontal overflow at 390px — PASS.** `/`: scrollWidth 390 / innerWidth 390 (`s8a2-home-390.png`). Story page: 390/390 (`s8a2-story-390.png`).
5. **Sprint ACs demonstrated by interaction — PASS** (attempt-1 full pass + this smoke, detail below).
6. **No regressions — PASS.** The attempt-1 failure is fixed. Fresh-load initial-focus highlight, per face, each in a brand-new browser context, read ~1.5s after `data-loaded="true"`:
   - `/` (default spawn, (-9.50, 0.00) heading 90): `data-focused` = king-who-chose-exile (ramayana), prompt visible; **698 emerald px** in the 360x380 center region vs **0 tint px** in both neighbor regions; turn-away drops center tint to **0** (unfocus still clears), turn-back restores **698** (identical to initial, so the first-load ring equals the known-good refocus ring). Zoom `s8a2-spawn-zoom.png`: emerald ring around the center painting, bare dark frames on both neighbors.
   - `/?face=roman` ((0,0) heading 0, wall 12 units): marcus-aurelius focused, **2301 amber px** center vs 0 in neighbors (`s8a2-roman-initial.png`, ring plainly visible at full frame). Attempt 1 measured **0** here; the race is gone.
   - `/?face=ramayana`: king-who-chose-exile focused, **2100 emerald px** center, 0 neighbors (`s8a2-ramayana-zoom.png`).
   - `/?face=mahabharata`: night-rage-burned focused, **2062 blue px** center, 0 neighbors (`s8a2-mahabharata-zoom.png`).
   - `/?face=curator`: `data-focused=""` (placard at view center, not a painting), prompt hidden, 0 tint px everywhere — the correct empty-stretch behavior, matching attempt 1.
   - Story page renders correctly at 1280 (`s8a2-story-1280.png`: cover, h1, tags, body) and 390 (`s8a2-story-390.png`); home renders at 390 with the focused painting wearing its emerald ring, touch cluster and prompt present (`s8a2-home-390.png`).

## Regression smoke of attempt-1 PASS evidence

- **Wheel walk (AC1):** `mouse.wheel(0, -300)` at `/?face=roman` moved (0,0) -> (0,-0.91), disp 0.910, direction dot 1.000, 11 distinct HUD frames; `wheel(0, 300)` back 0.900, dot -1.000. Page never scrolled: scrollY/X 0, `visualViewport.scale` 1, document not scrollable, before and after.
- **Wheel turn (AC2):** deltaX +240 -> heading +28.6, deltaX -240 -> -28.8; rendered view rotates (mean pixel diff 11.90/255).
- **Drag look (AC3):** 300 px right = +75.0 deg (0.250 deg/px), left = -75.0; vertical 0.125-0.150 deg/px; pitch clamps exactly at +25 / -25 after repeated big drags; releases of 300/40/6 px drags never changed `data-focused` or URL.
- **Gaze handoff while turning (AC4):** sweep right handed focus marcus -> (clear at 15.6) -> soldier -> brother-who-didnt-sleep -> vulture-wingless -> king-who-chose-exile -> god-who-forgot, in layout order.
- **Enter/Escape (AC4):** gaze-focused Enter opened the story (h1 matches slug) twice; settled round trip (`s8a2-roundtrip.js`: walk to (0.01,-2.10) heading 30, 1.5s settle) restored **dx 0.000, dz 0.000, dh 0.000**. One s8-gaze run read dh 0.200, but the save-time HUD read happened while turn inertia was still decaying (saved heading 0.2 settling toward 0); with a settled camera the restore is exact, so this is harness jitter, not a product regression. One harness flake where Escape pressed immediately at `networkidle` did not navigate; with a 1s settle it navigates reliably, and the unmodified s8-gaze.js path also succeeded this run.
- **Coexistence (AC5):** keyboard arrows drive the same HUD pipeline in all probes above; 390 touch context loads with the cluster visible (`s8a2-home-390.png`). Copy unchanged from attempt 1 (hint overlay text re-captured verbatim in the wheel run: "Arrow keys or scroll to walk · drag to look around · Enter to view a painting · Esc to step back", dismissed by first wheel tick).

## Soft notes (unchanged from attempt 1, carried to Sprint 9/10)

- No-WebGL fallback and CoverCarousel alt text still contain em dashes (Sprint 9 AC).
- Mobile shows keyboard copy; hint overlay overlaps the touch cluster at 390x844 (visible in `s8a2-home-390.png`; Sprint 9 AC).
- Taste flags for Sprint 10 stand: small paintings from room center, grabby corner focus, wheel-turn sensitivity, label spacing, washed-out header pill.

## Overall verdict: PASS

Gate 6 fix verified by pixel evidence on four independent fresh loads (698-2301 tint px where attempt 1 measured 0, neighbors at 0, initial count equal to the known-good refocus count). Gates 1-5 re-confirmed by smoke. Sprint 8 is done.
