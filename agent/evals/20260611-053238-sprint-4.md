# Sprint 4 verdict: Story triptych and Escape return

Date: 2026-06-11 05:32 UTC. Evaluated against agent/RUBRIC.md hard gates plus Sprint 4 acceptance criteria in agent/BACKLOG.md. Story page under test: `/20260421-053750-the-grandfather-who-could-not-choose-his-side`. Round-trip painting: `/20260328-203626-what-marcus-aurelius-wrote-in-the-dark`.

## Hard gates

1. **`pnpm build` exits 0** - PASS. `✓ Compiled successfully in 3.4s`, TypeScript clean, 15/15 static pages generated, exit code 0.
2. **Zero console errors** - PASS. Console + pageerror listeners on every run: homepage `[]`, `/gallery` `[]`, story page desktop `[]`, story page mobile (incl. swipes) `[]`, full round-trip journey `[]`, fresh-session direct load + Escape `[]`.
3. **No broken images** - PASS. `/gallery`: 11 imgs, all `naturalWidth > 0`. Story page: 3 panel imgs, each `naturalWidth = 1200`. Homepage renders via canvas, zero `img` elements (nothing to break).
4. **No horizontal overflow at 390px** - PASS. `scrollWidth == innerWidth == 390` on `/`, `/gallery`, and the story page (measured via `document.documentElement.scrollWidth`).
5. **Acceptance criteria by interaction** - PASS, all five (details below).
6. **No regressions at 1280px / 390px** - PASS. `shots/s4-home-1280.png` and `s4-home-390.png` show the lit Ramayana wall with framed paintings, Gallery view link, hint overlay. `s4-gallery-1280.png` / `s4-gallery-390.png` show the card grid with tradition filters and "Enter the museum" link. Story page renders at both widths (`s4-story-panel1-1280.png`, `s4-story-390-panel1.png`).

## Acceptance criteria evidence

**AC1 - 3-panel strip, distinct treatments, dots.** `[data-carousel]` contains 3 `[data-panel]`, all sourcing the full cover PNG. Panel 0: object-position 50% 50%, no transform/filter (full cover). Panel 1: scale(1.8), object-position 25% 40% (zoom crop). Panel 2: scale(1.4), object-position 75% 50%, `grayscale(1) sepia(0.35) contrast(1.05)`. Visibly distinct in `shots/s4-story-panel1-1280.png` / `-panel2-` / `-panel3-`. `[data-carousel-dots]` has 3 buttons; initial `aria-current` = [true, false, false].

**AC2 - Arrow cycling with wrap, clickable dots.** ArrowRight x4 from panel 0: scrollLeft/activeDot sequence (1280,1), (2560,2), (~0,0) wrap, (1280,1). ArrowLeft x2 from panel 1: (0,0) then wrap to (2557,2). Clicking dot 3 -> scrollLeft 2560, activeDot 2; clicking dot 1 -> back to panel 0. Active dot followed in every step.

**AC3 - 390px swipe, readable text, ArrowDown scrolls.** Real touch sequences (CDP `Input.dispatchTouchEvent`, viewport 390x844, hasTouch): swipe left -> scrollLeft 0->390, dot 0->1 (`shots/s4-story-390-panel2-swiped.png`); second swipe left -> 780, dot 2; swipe right -> 390, dot 1. ArrowDown x4 scrolled the page scrollY 0 -> 160. Story text readable at 390px (`shots/s4-story-390-text.png`). Tooling note: CDP `Input.synthesizeScrollGesture` is non-functional in this headless build (a vertical page gesture also moved nothing, scrollY 0->0), so raw touch events were used; the carousel itself responds correctly to genuine touch input.

**AC4 - Round trip position persistence.** From `/?face=roman` spawn (0.00, 0.00, 0.0): walked forward until `data-focused` set, turned slightly, settled. Noted (x=-0.00, z=-9.30, heading=350.0, focused=marcus-aurelius slug). Enter -> story URL opened. Escape -> back on `/`, HUD settled at (0.00, -9.30, 350.0), focus restored. Deltas dx=0.000 dz=0.000 dh=0.000, well within 0.1. Screenshots `shots/s4-roundtrip-before-enter.png` and `-after-escape.png` are visually identical views of the focused painting.

**AC5 - Direct load + Escape -> default spawn.** Fresh browser context, direct goto story URL, Escape -> landed on `/` at (x=-9.50, z=0.00, heading=90.0), exact default spawn, zero console errors. `shots/s4-directload-escape.png`.

## Soft notes (no fail)

- Dot states are accessible and styled: active = filled accent (`rgb(192,132,252)`), inactive = 1px muted outline, hover fills muted (`hover:bg-[var(--color-text-muted)]` verified by computed-style change on hover). Dots are small (10x10px) but visible on the dark theme at both widths.
- Panel 1 uses `object-fit: cover` in a 1280x450 strip, so the cover's extreme top/bottom edges crop at wide aspect ratios. The full composition (title art and caption band) still reads in the screenshot; acceptable as "the full cover" but worth knowing.
- Wrap-around scroll is smooth-animated; mid-animation scrollLeft readings (3, 11) settle correctly. No jank observed.
- Round-trip restores `data-focused` along with position, which is the right behavior (prompt reappears on return).

## Verdict

**PASS** - all 6 hard gates pass, all 5 acceptance criteria demonstrated by interaction.
