# Sprint 3 Verdict: Focus and Enter

Evaluated 2026-06-11 (UTC) against `agent/RUBRIC.md` hard gates and Sprint 3 acceptance criteria in `agent/BACKLOG.md`. Dev server: localhost:3000. Browser: headless Chromium via Playwright with `--use-gl=angle --enable-unsafe-swiftshader`.

## Hard gates

1. **`pnpm build` exits 0** — PASS. Build completed: "Compiled successfully in 3.3s", TypeScript clean, 15/15 static pages generated, exit code 0.
2. **Zero console errors** — PASS. Console + pageerror listeners on every run: homepage `/` (1280 and 390), story page `/20260328-203626-what-marcus-aurelius-wrote-in-the-dark` (both viewports), `/gallery` (both viewports), and all three interaction sessions on `/?face=roman` and `/?face=ramayana`. All error arrays empty.
3. **No broken images** — PASS. `naturalWidth > 0` on all rendered `img` elements: gallery 11/11 OK, story page 1/1 OK, homepage has 0 `img` elements (WebGL canvas, textures render in screenshots).
4. **No horizontal overflow at 390px** — PASS. `scrollWidth == innerWidth == 390` on `/`, story page, and `/gallery`. Shots: `agent/evals/shots/sprint3-reg-home-390.png`, `sprint3-reg-story-marcus-390.png`, `sprint3-reg-gallery-390.png`.
5. **All acceptance criteria demonstrated by interaction** — PASS, detail below.
6. **No regressions** — PASS. Homepage renders the lit museum room at 1280 and 390 (`sprint3-reg-home-1280.png`, `sprint3-reg-home-390.png`); story page renders cover + prose at both widths (`sprint3-reg-story-marcus-1280.png`, `-390.png`); gallery card grid intact (`sprint3-reg-gallery-1280.png`, `-390.png`).

## Acceptance criteria (gate 5 detail)

1. **Approach sets `data-focused` + prompt with title and Enter hint** — PASS. On `/?face=roman`, held ArrowUp from spawn (x=0, z=0); HUD reached z=-9.28 with `data-focused="20260328-203626-what-marcus-aurelius-wrote-in-the-dark"`, `#museum-prompt` display flipped none → flex, `#museum-prompt-title` = "What Marcus Aurelius Wrote in the Dark" (exact frontmatter title), prompt text includes "Press Enter to view". Same behavior on `/?face=ramayana` with a different correct slug (`20260331-054450-the-king-who-chose-exile-over-a-crown`). Shot: `sprint3-t1-focused-1280.png`.
2. **Focused painting visibly highlighted vs neighbors** — PASS. On the ramayana wall (5 paintings, neighbors in frame at focus distance): `sprint3-highlight-ram-focused-1280.png` shows the focused painting with a bright gold frame and spotlight while both visible neighbors stay dim/dark-framed. Boundary comparison `sprint3-highlight-ram-unfocused-1280.png` (same spot, one step earlier, x=8.66 vs 8.96) shows the same painting without the bright frame.
3. **Walking away / turning away clears focus and hides prompt** — PASS. Held ArrowDown: `data-focused` cleared to "" at z=-8.42, prompt display → none (`sprint3-t2-walked-away-1280.png`). Re-focused, then held ArrowLeft: focus cleared at heading 320.5 with position unchanged (z=-9.30), prompt → none (`sprint3-t3-turned-away-1280.png`).
4. **Enter while focused navigates to the story** — PASS. With `data-focused` set, pressed Enter; URL became `/20260328-203626-what-marcus-aurelius-wrote-in-the-dark` and the story rendered (`h1` = "What Marcus Aurelius Wrote in the Dark"). Shots: `sprint3-t5-pre-enter-1280.png`, `sprint3-t5-story-after-enter-1280.png`.
5. **Enter with nothing focused is a no-op** — PASS. With `data-focused=""` (after turning away), pressed Enter; URL stayed `http://localhost:3000/?face=roman`, zero console errors over the following 2s.

## Soft notes (no fail)

- Focus engages with roughly 2.0-2.3 units of forward travel remaining before the collision stop, slightly tighter than the spec's nominal "distance < 3.0" (likely measured to the painting plane). UX is fine: focus lands comfortably before the wall stop. Worth a glance if corner paintings ever feel hard to focus.
- Prompt pill (`sprint3-prompt-crop.png`): white title, purple "Enter" accent, purple border. Legible. The gray "— Press ... to view" portion is on the dim side but readable at full size.
- The prompt accent is site-purple rather than the focused painting's tradition color (amber/emerald/blue). Consistent with the existing "Enter the museum" button styling, so acceptable; a tradition-tinted prompt would be a nice Sprint 5 atmosphere touch.
- First-visit hint overlay now reads "Arrow keys to walk and turn · Enter to view a painting · Esc to step back" (`sprint3-reg-home-390.png`), correctly updated for this sprint's feature.

## Overall: PASS

All six hard gates pass. No fix instructions required.
