# Verdict — Sprints 16 + 17, attempt 2 (focused re-evaluation)

Date: 2026-06-12 04:27 UTC. Mode: verdict, focused scope per task. Attempt 1 (`20260612-040143-sprint-16-17.md`) passed 9 of 10 gates; only Sprint 17 AC3 (B3) failed. Since then the builder applied the prescribed MuseumRoom subscription re-sync and pinned trystero 0.25.2 -> 0.21.8 (installed 0.21.8 confirmed in `node_modules/trystero/package.json`). This re-run verifies exactly four items; attempt-1 evidence stands for everything else. Scripts: `agent/evals/tmp/s1617-b3-avatars.js`, `s1617-b3b-reload.js`, `s1617a2-b3c-reload2.js`, `s1617a2-smoke-local.js`, `s1617a2-idle.js`, `s1617-b5-prod.js`. Shots: `agent/evals/shots/s17-b3b-*`, `s17a2-*`, `s17-b5-prod-*`.

## Overall: PASS — all four re-verification items hold; the attempt-1 FAIL is fixed and the transport swap introduced no regression in any path exercised

## Item 1 — Sprint 17 B3 (the failed gate): stored avatar variant applied to the player's own body — PASS

- Fresh profile assignment: fresh context got `museum.avatar.v1` = "0", second fresh context "3"; both in 0-3 (`s1617-b3-avatars.js` FRESH/FRESH2).
- B cycles all 4: localStorage walks 0→1→2→3→0 across 5 presses; value survives reload (before "1" / after "1"); zero console errors.
- **The fix itself:** with `museum.avatar.v1='3'` preset on `?net=off`, the rendered torso samples mustard [135,100,22] / [134,100,22] / [134,99,22] at +1 s, +3 s, +6 s after `data-loaded` — attempt 1 measured navy [24,30,41] at all three points. Screenshot `s17-b3b-preset3-1000.png`: mustard shirt, shaved head, broader build — the variant-3 body, visually confirmed. Pressing B cycles to navy [24,30,41] (ls "0"), and a reload with stored "3" (the init script re-pins it) renders mustard again [134,99,22] (`s1617-b3b-reload.js`).
- Second variant per task: preset `'2'` renders maroon [85,43,42] at +1 s, [78,36,35] at +3 s, and [82,40,39] after a straight reload (`s1617a2-b3c-reload2.js`; `s17a2-b3c-preset2-reload.png` shows the maroon shirt / brown hair / blue jeans body). Never navy.
- Zero console errors in all B3 sessions (ERRS_B3 [], ERRS_B3B [], ERRS_B3C []).
- Self-view now matches what peers see via wire `v` (B4, attempt 1). Gate closed.

## Item 2 — Sprint 17 B5 production e2e on trystero 0.21.8 — PASS

`s1617-b5-prod.js`, chromium with `--ignore-certificate-errors`, two SEPARATE contexts on plain `http://localhost:3000/`:

- Both settled `data-net="webrtc"` in 1.8 s.
- Mutual discovery `data-peers="1"` both sides 7.4 s after load (limit ~30 s); chips "2 in the room"; peer nodes live (`s17-b5-prod-A/B.png`).
- A's walk streamed to B over the real link: 10 distinct peer poses.
- Real relays on wss: relay.nostromo.social, relay.fountain.fm, relay.verified-nostr.com, nostr.sathoarder.com, nostr.cool110.xyz (0.21.8's default pool differs from the 0.25 fork's).
- Console: 3 (A) / 2 (B) messages, ALL matching the calibrated exempt class from `20260611-235012-sprint-13-criteria-r2.md` (`/WebSocket connection to .+ failed/`, browser network layer): wss://nostr.cool110.xyz answers HTTP 200 instead of upgrading. Zero non-exempt errors, zero pageerrors, both sides.

## Item 3 — Idle discoverability (Monte's reported bug) — PASS

`s1617a2-idle.js`: context A loaded plain `/`, settled `webrtc` at 3.7 s, then idled ALONE for 201 s (20 samples at 10 s intervals: net "webrtc", peers "0" throughout — past the 0.25 fork's ~2-minute deafness threshold). Fresh context B then joined:

- B saw A at 11.5 s after B's `data-loaded`; A saw B at 11.8 s. Both within ~30 s.
- Final state both sides: peers "1", total "1", peer state "live", chip "2 in the room" (`s17a2-idle-A/B.png`). The idle visitor is no longer invisible to newcomers.
- Console over the full ~4 min window: A 11 messages, B 2, every one in the exempt class (handshake failures to two dead public relays: cool110.xyz → 200, verified-nostr.com → 522). Zero non-exempt errors, zero pageerrors.

**Calibration note (no gate, flag for next alignment):** 0.21.8's announce-forever design keeps retrying dead relays indefinitely — A logged exempt-class messages at t=58.4 s through t=221.2 s at a slow taper (~1 per 20-50 s, 11 total in 4 min, far under any spam threshold). This violates the letter of sprint 13 AC4's cessation bound ("none later than 45 s after load"), which was calibrated against the give-up behavior that caused this very bug. The persistence IS the fix. The bound should be re-calibrated to a rate cap (e.g. < N per minute steady-state) instead of a cessation deadline at the next criteria alignment.

## Item 4 — Smoke — PASS

- `pnpm build` exit 0 (BUILD_EXIT=0, `/tmp/build-attempt2.log`; 15/15 pages generated).
- Solo room (`?net=off`): loads clean, net "off", peers "0", zero console errors (`s17a2-smoke-solo.png`).
- `?net=local` two-page flow (`s1617a2-smoke-local.js`, one context/two pages as BroadcastChannel requires): mutual discovery peers 1/1, chip "2 in the room"; A's walk streamed to B (10 distinct poses, `s17a2-smoke-local-B.png`); A leaves → B drops to peers "0", the lingering `.museum-peer` node is the despawn animation (state "leaving", removed within ~600 ms — verified by a 300 ms-interval timeline sample). Zero console errors on both pages.

## Decision

All four items PASS. Attempt-1 evidence stands for the other 8 gates (Sprint 16 A1-A5, Sprint 17 B1, B2, B4). Hard-gate summary: build PASS, console PASS (zero non-exempt everywhere), images/overflow/regressions unchanged from attempt 1 PASS, all 10 ACs now demonstrated by interaction.

**Overall: PASS.**

Carried-over nice-to-haves from attempt 1 (unchanged, no gates): wall-stand pitch-down composition, B-key discoverability/touch path, fast-turn camera distance cuts, the one-off invisible-rejoin render. New from this run: the stale sprint-13 cessation bound above.
